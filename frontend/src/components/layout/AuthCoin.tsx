import styles from './AuthBrand.module.css'

/** Mascot used on every pre-authentication page (Login is the visual reference). */
export function AuthCoin() {
  return <img src="/assets/Coin.png" alt="Coin1" className={styles.Coin} />
}
