import styles from './AppBrand.module.css'

/** Mascot used on every authenticated page (Principal is the visual reference). */
export function AppCoin() {
  return <img src="/assets/Coin.png" alt="Mascote DinControl" className={styles.Coin} />
}
